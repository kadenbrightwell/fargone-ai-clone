//required
pfs[0] = [-1, -1, -1, -1]; //faces the player can interact from

//optional
	default_result = function(){};
result[0] = default_result; //default no effect when interacted with
	default_maxdtp = 2;
maxdtp[0] = default_maxdtp; //maximum pixel distance (can be changed per object)
